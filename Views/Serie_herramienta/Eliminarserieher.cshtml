@model CONTROL_HERRAMIENTA_SUNCORP.Models.Serie

@{
    ViewBag.Title = "Eliminarserieher";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" />
<div class="row" style="margin-top:30px">
    @if (ViewBag.ErrorMessage != null)
    {
        <div class="alert alert-danger">
            @ViewBag.ErrorMessage
        </div>
    }
    <div class="col-sm-12 text-center">
        <!-- Agregué la clase text-center -->
        <div class="panel panel-warning" style="display: inline-block;">
            <!-- Agregué display: inline-block -->
            <div class="panel-heading">
                <h4>ELIMINAR SERIE HERRAMIENTA</h4><br />
            </div>
            <div class="panel-body">
                <div class="alert alert-danger">
                    ¿Desea eliminar la serie @Html.DisplayTextFor(m => m.NUMERO_SERIE) ?
                </div>

                <form action="@Url.Action("Eliminarserieher","Serie_herramienta")" method="post">
                    <input type="hidden" class="form-control" name="Idserie" value="@Html.DisplayTextFor(m => m.ID_REGISTRO_SERIE_HERRAMIENTA)" />

                    <button type="submit" class="btn btn-danger" style="font-size: 20px; color: #FFFFFF;">
                        <i class="fas fa-trash-alt" aria-hidden="true"></i>
                    </button>
                    <a class="btn btn-warning" href="@Url.Action("serie","Serie_herramienta")" style="font-size: 20px; color: #FFFFFF;">
                        <i class="fas fa-arrow-left" aria-hidden="true"></i>
                    </a>
                </form>

            </div>
        </div>
    </div>
</div>



